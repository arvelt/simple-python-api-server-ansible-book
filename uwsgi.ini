# deploy to /home/vagrant/
[uwsgi]
socket = /tmp/uwsgi.sock
http = :9090
callable = app
chdir = /home/vagrant/hello-flask
wsgi-file   = main.py
uid         = vagrant
gid         = vagrant
processes = 4
threads = 2
